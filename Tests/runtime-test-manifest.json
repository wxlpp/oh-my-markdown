{
  "ios18": {
    "requiredTargets": [
      "MarkdownKitTests",
      "MarkdownMathTests",
      "ExampleTests",
      "ExampleUITests"
    ],
    "requiredTests": [
      "Parsed blocks include stable source fingerprints",
      "合法公式 → .rendered，尺寸为正",
      "Smoke markdown parses one heading",
      "testRootDemoIsAccessible()"
    ]
  },
  "macos15": {
    "requiredTargets": [
      "MarkdownKitTests",
      "MarkdownMathTests"
    ],
    "requiredTests": [
      "Parsed blocks include stable source fingerprints",
      "合法公式 → .rendered，尺寸为正"
    ]
  }
}
